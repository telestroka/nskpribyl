<nav>	<ul class="ns">		<?php            if (count($items) > 1) {                foreach ($items as $menu_id => $menu_params) {                    if ($menu_id == $id)                        echo '<li class="active">' . $menu_params['title'] . '</li>';                    else echo '<li><a href="' . $site->createUrl('articles', 'item') . '?id=' . $menu_id . '">' . $menu_params['title'] . '</a></li>';                }            }		?>	</ul></nav>